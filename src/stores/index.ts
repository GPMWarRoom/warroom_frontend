import { defineStore } from 'pinia'

export const versionStore = defineStore('version', {
    state: () => ({
    }),

    actions: {
        async fetchVersions() {
        }
    },

    getters: {
        
    }
})
